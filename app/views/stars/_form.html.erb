<%= form_for([@volunteer, @star]) do |f| %>
  <% if @star.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@star.errors.count, "error") %> prohibited this volunteer from being saved:</h2>

      <ul>
      <% @star.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <% if false %>
    <div class="field">
      <%= f.label :activity, "What did they do?" %><br />
      <%= f.text_field :activity %>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :board_member, "What was their role?" %><br />
    <%= f.select(:board_member, [["Volunteer",false],["Board Member",true]])%>

  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
